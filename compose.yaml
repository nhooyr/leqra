services:
  game:
    build: .
    ports:
      - "8080:8080"
    environment:
      PORT: "8080"
      MAX_ROOMS: "64"
    restart: unless-stopped
    read_only: true
    cap_drop:
      - ALL
    security_opt:
      - no-new-privileges:true
